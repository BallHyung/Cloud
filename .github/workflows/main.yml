name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # เปลี่ยนเป็นชื่อ branch ที่คุณใช้ (เช่น master)

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 
        uses: actions/checkout@v3

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
       # with:
        #  github_token: ${{ secrets.GITHUB_TOKEN }}
         # publish_dir: ./  # โฟลเดอร์ที่เก็บไฟล์ (ในกรณีนี้คือ root folder)
